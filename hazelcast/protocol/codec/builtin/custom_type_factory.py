from hazelcast.core import Address

class CustomTypeFactory:
    @staticmethod
    def create_address(host, port):
        pass

    @staticmethod
    def create_simple_entry_view(key, value, cost, creation_time, expiration_time, hits, last_access_time, last_stored_time, last_update_time, version, ttl, max_idle):
        pass